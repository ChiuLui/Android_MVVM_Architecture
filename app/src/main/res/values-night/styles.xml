<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/color_primary_night</item>
        <item name="colorPrimaryDark">@color/color_primary_dark_night</item>
        <item name="colorAccent">@color/color_accent_night</item>
    </style>

    <style name="Theme.AppTheme" parent="Base.Theme.AppTheme"/>

    <style name="Theme.Login.EditText" parent="Theme.AppTheme">
        <item name="colorControlNormal">@color/transparent</item>
        <item name="colorControlActivated">@color/color_accent_night</item>
    </style>

</resources>